from setuptools import setup

setup(name='tag2network',
      version='0.1',
      description='Build similarity network from tagged documents',
      url='http://github.com/foodwebster/tag2network',
      author='Rich Williams',
      license='MIT',
      packages=['tag2network'],
      zip_safe=False)
